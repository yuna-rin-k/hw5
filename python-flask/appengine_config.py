from google.appengine.ext import vendor

# Add any libraries install in the "lib" folder.
vendor.add('lib')
# To install all the libraries specified in "requirements.txt" to the "lib" folder, use
#   pip install -t lib -r requirements.txt
